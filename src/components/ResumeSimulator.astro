---
import { LINKS } from '../config/links';
import { useTranslations } from '../i18n/utils';
import type { Locale } from '../i18n/types';

interface Props {
  lang?: Locale;
}

const { lang = 'en' } = Astro.props;
const t = useTranslations(lang);

const features = [
  { icon: '\u2728', text: t('resumeSimulator.feature1') },
  { icon: '\u270F\uFE0F', text: t('resumeSimulator.feature2') },
  { icon: '\uD83D\uDCCA', text: t('resumeSimulator.feature3') },
  { icon: '\uD83C\uDFAF', text: t('resumeSimulator.feature4') },
];
---

<section class="bg-white py-16 md:py-24">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="grid items-center gap-12 lg:grid-cols-2 lg:gap-16">
      <!-- Content (Left on desktop) -->
      <div class="max-w-lg">
        <h2 class="font-display text-3xl font-bold text-text-primary sm:text-4xl">
          {t('resumeSimulator.title')}
        </h2>

        <p class="mt-6 text-lg leading-relaxed text-text-body">
          {t('resumeSimulator.description')}
        </p>

        <!-- Features List -->
        <ul class="mt-8 space-y-4">
          {
            features.map((feature) => (
              <li class="flex items-center gap-3">
                <span class="text-xl">{feature.icon}</span>
                <span class="text-text-body">{feature.text}</span>
              </li>
            ))
          }
        </ul>

        <!-- CTA Button -->
        <a
          href={LINKS.getStarted}
          data-cta-link="signup"
          data-umami-event="resume_simulator_getstarted_click"
          class="mt-8 inline-flex items-center gap-2 font-medium text-primary transition-colors hover:text-primary-hover"
        >
          {t('resumeSimulator.getStarted')}
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </a>
      </div>

      <!-- Image (Right on desktop) -->
      <div class="relative">
        <div
          class="aspect-[4/3] w-full overflow-hidden rounded-2xl bg-gradient-to-br from-gray-100 to-gray-200 shadow-2xl"
        >
          <img
            src="/images/alignment-lab.webp"
            alt="Alignment Lab showing resume comparison with original, tailored, and edited versions"
            class="h-full w-full object-cover"
            width="1200"
            height="864"
            loading="lazy"
          />
        </div>

        <!-- Decorative elements -->
        <div class="absolute -right-4 bottom-8 text-lg text-primary/30">+</div>
        <div class="absolute -right-8 top-1/2 text-xl text-alert/30">+</div>
      </div>
    </div>
  </div>
</section>
